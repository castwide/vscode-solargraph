<%= tag.name %>
<% unless tag.types.nil? or tag.types.empty? %>
	[<%= tag.types.join(', ') %>]
	<% unless tag.text.empty? %>
		-
	<% end %>
<% end %>
<% unless tag.text.empty? %>
	<%= tag.text %>
<% end %>
